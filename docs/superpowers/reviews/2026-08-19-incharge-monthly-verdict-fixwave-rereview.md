FINDING 1: ADDRESSED — `cancelStaffBills` now takes optional `dueFrom`/`dueTo`, cron passes `window.start`/`window.end`; omitted-window callers keep whole-year behavior (tested).

FINDING 2: ADDRESSED — `canAct = atMonthEnd || monthExplicit`, `act = mode==='enforce' && !dryRun && canAct`; shadow recording still happens unconditionally, only the write/cancel/bill/revoke/notify side effects are withheld, with `actionWithheldReason` surfaced in the summary.

FINDING 3: ADDRESSED — cron's probation query now adds `.gte('window_end', window.start).lte('window_start', window.end)` so only probations overlapping the verdict month narrow/pass it. Filter shape isn't byte-identical to `/api/boarding/access` (which does an unbounded `gte(window_end)` plus JS status filtering, no upper bound), but the substance of the bug — an old active probation from an earlier month silently narrowing a later one — is closed.

FINDING 4: ADDRESSED — shadow/dryRun PASS-path preview now calls `loadStaffBillState` and only reports `billAction: 'cancelled'` when `state.hasOutstanding`, else `'none'`.

FINDING 5: ADDRESSED — assignments are grouped by lowercased `staff_email` into a Map, evaluated once via the union of routes (service days and marks merged into shared sets before `evaluateMonth`), and on fail all of the person's assignment ids are deactivated in one `.in('id', assignmentIds)` update before `maybeRevokeBoardingRole` is called.

FINDING 6: ADDRESSED — on a fail+bill+revoke, the loop now updates `tms_incharge_attendance_strike` (`removed_at`, `billing_status: 'billed'`) for every deactivated assignment id, update-only (no insert), so a missing strike row is skipped rather than fabricated.

FINDING 7: ADDRESSED — before upserting, if the freshly computed `billAction` is `'none'`, the route reads the existing verdict row's prior `bill_action` and keeps it if it was non-`'none'`, preventing a re-run from downgrading `'cancelled'`/`'generated'` back to `'none'`.

FINDING 8: ADDRESSED — no `logActivity` call was added to the cron route; instead a route-header comment explicitly documents that this route has no `AuthContext` and that the `tms_incharge_month_verdict` row is the deliberate audit substitute. Matches the requirement (documented deviation, not an invented AuthContext).

FINDING 9: ADDRESSED, migration remains UNAPPLIED — the `leaked` CTE in `20260818110000_revoke_billed_incharge_reassignments.sql` now excludes anyone with `exists (select 1 from tms_incharge_probation p where status='active' and email matches)`. The file's top-of-file "Do NOT apply" language is still present.

FINDING 10: ADDRESSED, migration remains UNAPPLIED — `20260818120000_tms_incharge_month_verdict_cron.sql` now has a comment block stating the crude `28-31` schedule is only safe paired with the route's `canAct`/date guard (finding 2), and warns against "simplifying" the schedule without checking that guard.

FINDING 11: ADDRESSED — `app/api/boarding/incharge-pledge/route.ts` now re-derives `allowed`, `hasOutstandingBill` (via `resolveStaffId` + `loadStaffBillState`, gated on `staffId && currentYear?.id`, matching `/api/boarding/access`), `probationThisMonth`, and `remainingServiceDays` server-side, feeds them through the same `deriveInChargeGate`, and 403s unless the result is exactly `'pledge'`. A second explicit check rejects `windowServiceDays.length === 0` before insert, closing the zero-service-day auto-pass. Ordering (eligibility → route → gate → zero-day check → probation insert → grant) and the pre-existing rollback path are unchanged.

FINDING 12: ADDRESSED — the by-person mark-paid route now: filters to bills with `status='generated'` only (never touches `staff_deferred`, matching the older per-bill route's refusal), sets `status: 'paid'`, `paid_amount` (summed from real `amount`), `payment_reference`, and guards the update with `.eq('status','generated')` for concurrent-double-mark safety — same column semantics and same guard as `app/api/admin/fees/[id]/staff-bills/mark-paid/route.ts`.

FINDING 13: ADDRESSED — `app/(admin)/staff-route-assignments/enforcement/page.tsx` adds one `<Link href="/staff-route-assignments/enforcement/monthly">Monthly verdict board</Link>` inline in the existing explanatory paragraph; diff shows no other change to that file, and `app/(admin)/staff-route-assignments/enforcement/monthly/page.tsx` + `columns.tsx` already exist in the tree.

FINDING 14: ADDRESSED — `/api/boarding/access` now computes `feeCheckReason` (`'no_current_year'` | `'staff_unresolved'` | `null`) mirroring self-assign's fail-closed conditions, and forces `gate = 'denied'` when the computed gate is `'choose'` and `feeCheckReason` is set, publishing `reason` in the response. The outer try/catch's fail-closed fallback (`gate: 'denied'` on any exception) is untouched.

FINDING 15: ADDRESSED — `lib/boarding/access-state.ts` and `lib/boarding/access-state.test.ts` are deleted; `lib/boarding/incharge-gate.ts`'s header comment now states it is "the sole authority" and explains the deleted file had zero live call sites. Confirmed via grep: no reference to `access-state` or `deriveBoardingAccess` remains anywhere in the live tree (`app/`, `lib/`) — only a stale, unrelated worktree (`.worktrees/auto-bill-on-onboarding`, a different in-progress branch) and the `.next/standalone` build artifact still mention it, neither of which is part of this diff's scope.

NEW BREAKAGE: none

Checked specifically:

- **Group-by-person restructure** (`app/api/cron/incharge-month-verdict/route.ts`): route-level credit is preserved — `serviceDaySet`/`markedSet` are unioned across all of the person's route ids before a single `evaluateMonth` call, so a mark on ANY of their routes on a given day counts. Probation window narrowing still applies (one probation lookup per group, keyed on `group.staffEmail`, now correctly month-filtered per finding 3). Booking/attendance loads still fail loud (`routeDates` still throws on `attErr`/booking errors) and are still caught per unit of work — the unit of work is now the person, not the assignment row, which is an intended and necessary consequence of the fix (a person with two routes, one of which errors, now has their whole verdict deferred to the next run rather than one route silently evaluating in isolation — this is correct: you cannot pass/fail someone on an incomplete union of their routes). `act` gating is untouched — same `if (act) {...} else {...}` shape, now operating on the group instead of the row.
- **Pledge gate false-403 risk** (the one asked to check hardest): compared `app/api/boarding/incharge-pledge/route.ts`'s re-derivation input-by-input against `app/api/boarding/access/route.ts`: same `getStaffBoardingEligibility(auth.supabase, auth.userId)` call, same `hasScan`/`getAssignedRouteIdsForUser` → `allowed`, same `currentYear` lookup, same `resolveStaffId(svc, { email, profileId: auth.userId })`, same `loadStaffBillState` gated on `staffId && currentYear?.id`, same probation query shape (`ilike` + `gte(window_end, win.start)`) and same booking/`serviceDays` computation for `remainingServiceDays`, fed into the identical `deriveInChargeGate`. A person `/api/boarding/access` would show `'pledge'` will get `'pledge'` from the pledge route's own derivation too — no legitimate pledger is stranded by this gate. (Access route's separate `feeCheckReason`-forces-`denied` logic only affects the `'choose'` outcome, which is irrelevant to the pledge path.)
- **Mark-paid change**: the two mark-paid routes (by-person and by-bill) now share identical semantics — both filter to `status='generated'`, both write `status:'paid'`+`paid_amount`+`payment_reference`+`marked_paid_by`, both guard with `.eq('status','generated')`. Neither can act on a bill the other has already settled or on a `staff_deferred` held bill, so no double-write or state-skew is possible between them.
- **Scope check**: `git diff --stat` (from the package header) touches exactly the 12 files listed, all within the two waves' stated scope (mark-paid parity, pledge gate, cron restructure/canAct/dedup, access-route mirroring, board link, access-state deletion, cancel-bill windowing, both migrations). No migration was applied — both retain their "Do NOT apply" language and no `apply_migration` call was made during this review. No unrelated file was touched.

MERGE READINESS: All 15 findings are addressed with fixes that match their stated requirements, and none of the restructuring (grouping by person, the new server-side pledge gate, or the mark-paid parity change) introduces new breakage — the pledge-gate re-derivation in particular is a faithful field-for-field mirror of the access route's own gate, so it cannot false-403 anyone the access endpoint would itself offer `'pledge'` to. Combined with the controller's independent build/test verification (74 files / 818 tests, `next build` clean, no dangling imports) and this review's confirmation that both migrations remain unapplied and no scope creep occurred, this fix wave is ready to merge as-is, still shipping inert in `shadow` mode with nothing deployed.
